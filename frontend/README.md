# OeniVault AI — Frontend

React 18 + TypeScript + Tailwind + Vite SPA.

```bash
cp .env.example .env
npm install
npm run dev
```

Demo API expects backend at `VITE_API_URL` (default `http://localhost:4000`).
